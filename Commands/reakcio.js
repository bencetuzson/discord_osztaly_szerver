const Discord = require('discord.js');
const setup = require('../../database/setup.json');

module.exports = {
    name: 'reakcio',
    description: 'gives role when reacted',
    execute(message, args){
      
    }
}